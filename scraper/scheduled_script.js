// scraper.js
// Calls Scraper API and uses regex to extract current price, also checking for any current deals

// Import .onv variables from local file
require('dotenv').config();

const mongoose = require('mongoose');

const config = require('./config');

// Get list of all watched products

// Loop through each

// Scrape the specific page

// Regex to get "Current Price"

// Check to see if "Deal Price" is on the page

// Store updated fields back to relevant document

//! IMPORTANT
mongoose.connection.close();
